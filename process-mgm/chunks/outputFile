structPath(char* path) {
    char procDir[] = "/proc/";
    char fdDir[] = "/fd";
    char pid[8];
    sprintf(pid, "%d", getpid());

    strcat(path, procDir);
    strcat(path, pid);
    strcat(path, fdDir);
}

