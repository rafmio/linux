#inlcude <unistd.h>
int close(int fd);

Возвращает:
	- при успешном завершении 0
	- при ошибке -1

	Обычно предпочтительнее явно закрывать ненужные fd. Будет надежнее. 
	Как и др.системные вызовы close() должен всегда проверяться на ошибки.


